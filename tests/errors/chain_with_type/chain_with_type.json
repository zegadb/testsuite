{
  "title": "A walk that names the type",
  "stage": "parse",
  "hosts": ["native", "browser"],
  "notes": "The old `rel -> Type(…)` in a filter is refused; the help writes the chain, since the schema knows the type (zega#86).",
  "knownFailure": {
    "reason": "zegadb/zega#86: the pinned engine (2b5ebd2) has no has/!have/in/with/same chains or N hops; this case records its outcome until the pin moves.",
    "observed": {
      "ok": false,
      "stage": "parse",
      "stdout": "",
      "stderr": "error: expected a comparison after route\n  schema:142:17\n    Airport(route -> Airport(country -> Country(iso = \"JP\"))) { code }\n                  ^^\n  help: use `=`, `!=`, `>`, `<`, `>=`, `<=`, `<>`, `findExact`, `findLike`, `startsExact`, `startsLike`, `endsExact`, or `endsLike`\n"
    }
  }
}
