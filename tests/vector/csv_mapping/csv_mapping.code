schema {
  type Ticket {
    n: Int
    embedding: Vector<3> from (x, y, z)
  }
}

mutation csv ["rows.csv"] {
  Ticket(n: $n) { n }
}

query {
  Ticket @near(embedding, @vector[1, 0, 0], 1, exact) { n embedding }
}
