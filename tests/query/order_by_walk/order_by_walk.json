{
  "title": "Order and limit on a walk target",
  "stage": "run",
  "hosts": ["native", "browser"],
  "notes": "`order by` and `limit` apply to the nodes a relationship reaches, not only to the root (zega#73).",
  "knownFailure": {
    "reason": "zegadb/zega#73: the pinned engine has no order by a field and no delete",
    "observed": {
      "ok": false,
      "stage": "parse",
      "stdout": "",
      "stderr": "error: expected @distance\n  schema:54:32\n      players -> Player order by salary limit 2 { name }\n                                 ^^^^^^\n"
    }
  }
}
