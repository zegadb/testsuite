{
  "title": "URL fields reject userinfo",
  "stage": "run",
  "hosts": [
    "native",
    "browser"
  ],
  "notes": "String<url> requires an absolute HTTP(S) URL with a host and no userinfo. Reject this value before writing a node."
}
