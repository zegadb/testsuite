{
  "title": "One related node is enough",
  "stage": "run",
  "hosts": ["native", "browser"],
  "notes": "A walk over a to-many relationship holds when any related node matches: YYC flies to NRT and AMS, and one Dutch destination is enough (zega#86).",
  "knownFailure": {
    "reason": "zegadb/zega#86: the pinned engine (2b5ebd2) has no walk in a condition and no @count; this case records its outcome until the pin moves.",
    "observed": {
      "ok": false,
      "stage": "parse",
      "stdout": "",
      "stderr": "error: expected a comparison after route\n  schema:130:17\n    Airport(route -> Airport(country -> Country(iso = \"NL\"))) { code }\n                  ^^\n  help: use `=`, `!=`, `>`, `<`, `>=`, `<=`, `<>`, `findExact`, `findLike`, `startsExact`, `startsLike`, `endsExact`, or `endsLike`\n"
    }
  }
}
