{
  "title": "in: one more hop",
  "stage": "run",
  "hosts": ["native", "browser"],
  "notes": "`has route in country(iso = \"JP\")` walks a route, then that airport's country; joined with `&&` to a test on the airport's own country (zega#86).",
  "knownFailure": {
    "reason": "zegadb/zega#86: the pinned engine (2b5ebd2) has no has/!have/in/with/same chains or N hops; this case records its outcome until the pin moves.",
    "observed": {
      "ok": false,
      "stage": "parse",
      "stdout": "",
      "stderr": "error: expected a comparison after has\n  schema:142:15\n    Airport(has country(iso = \"CA\") && has route in country(iso = \"JP\")) { code }\n                ^^^^^^^\n  help: use `=`, `!=`, `>`, `<`, `>=`, `<=`, `<>`, `findExact`, `findLike`, `startsExact`, `startsLike`, `endsExact`, or `endsLike`\n"
    }
  }
}
