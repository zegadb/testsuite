{
  "title": "Delete with @detach",
  "stage": "run",
  "hosts": ["native", "browser"],
  "notes": "`@detach` removes a deleted node's relationships with it; the team is left with no players (zega#73).",
  "knownFailure": {
    "reason": "zegadb/zega#73: the pinned engine has no order by a field and no delete",
    "observed": {
      "ok": false,
      "stage": "parse",
      "stdout": "",
      "stderr": "error: expected }\n  schema:41:10\n    delete Player(name = \"Ada\") { @detach }\n           ^^^^^^\n"
    }
  }
}
