schema {
  type Person {
    name: String
    age: Int
    active?: Bool
    score?: Float
    friends -> Person[]
  }
}

mutation {
  Person(name: "Ada" && age: 30)
}

query {
  Person {
    name
    age
  }
}
