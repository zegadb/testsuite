{
  "title": "Delete every match, returning ids and fields",
  "stage": "run",
  "hosts": ["native", "browser"],
  "notes": "`delete Player(salary = 300)` deletes both matches and returns their count, with `@id` and `name` read before the delete (zega#73)."
}
