schema {
  type Person {
    name: String
    age: Int
    active?: Bool
    score?: Float
  }
}

mutation csv ["./people.csv"] {
  Person(name: $"Full Name" && age: $Age)
}

query {
  Person {
    name
    age
  }
}
